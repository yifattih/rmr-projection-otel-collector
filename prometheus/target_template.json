[
    {
        "targets": ["${TARGET_URI}"],
        "labels": {
            "deployment": "${DEPLOYMENT}",
            "environment": "${ENVIRONMENT}",
            "service": "${SERVICE}"
        }
    }
]